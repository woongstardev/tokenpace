# BRIEF — tokenpace

- 상태: **v1 (2026-09-01)** — 조사·설계
- 발원: "35/10/5 tok/s 체감 사이트, 이미 있는지 조사 + 어디에 띄울지 설계"
- 이 문서가 **범위의 정본**이다. 바뀌면 여기를 고치고 커밋해라 — 결정은 대화가 아니라 문서에 남는다.

---

## 1. 한 줄

**tokenpace** — LLM의 초당 토큰 수(tok/s)가 *내 언어로, 내가 읽는 속도에 견줘* 얼마나 빠른지 몸으로 느끼게 하는
정적 웹 페이지. 한국어를 1급 언어로 두되 **영어 사용자도 그대로 쓸 수 있게(i18n) 만든다.**

## 2. 왜 만드나 — 선행 조사 결과 (2026-09-01)

### 2-1. 같은 범주는 이미 포화다

| 사이트 | 특징 |
|---|---|
| https://shir-man.com/tokens-per-second/ | 5/25/67 t/s 프리셋, `?speed=15` URL 파라미터. **TTFT는 명시적으로 제외**한다고 밝힘 |
| https://tokenstead.ai/tokens-per-second | 10/50/100/500 나란히 동시 재생 |
| https://github.com/jimmyboudoux/token-speed-visualizer | 2~4레인 레이스, 1~500 커스텀, **바닐라 HTML/CSS/JS·의존성 0·빌드 없음**, CSP 엄격·외부요청 0. 벽시계 기준 타이밍(누적 드리프트 회피). 파이썬 구문 강조 |
| https://tps-simulator.com/ · https://mikeveerman.github.io/tokenspeed/ · https://float16.cloud/tokenspeed/ · https://nickmimic.github.io/llm-speed-simulator/ · https://kamilstanuch.github.io/LLM-token-generation-simulator/ · https://techtactician.com/llm-tokens-per-second-inference-speed-simulator-tool/ | 대동소이 |

⇒ **"속도만 눈으로 보여주는 것"으로는 신규성 0.** 그 축으로 경쟁하지 마라.

### 2-2. 전부가 비운 세 구멍 = 우리 제품의 존재 이유

1. **언어별 토큰 밀도.** 조사한 전부가 영어 기준이다. 영어는 대략 1토큰 ≈ 4자, 한국어는 1토큰 ≈ 1~2자
   (모델·토크나이저별로 다르다 — **우리가 실측해서 수치를 박는다**). 같은 35 tok/s가 영어로는 초당 ~140자,
   한국어로는 초당 ~35~50자 — **체감이 3배 가까이 갈린다.** 벤치마크의 tok/s 숫자를 한국어 사용자가
   그대로 믿으면 반드시 실망한다. 이 축을 다루는 사이트가 하나도 없다.
2. **TTFT(첫 토큰까지의 대기).** shir-man은 "초기 대기 제외"라고 못박았고 나머지도 안 다룬다. 실제 체감은
   `TTFT + 디코딩 속도`다. 로컬 LLM은 모델 로드·프리필이 크다 — 100 tok/s여도 8초 기다리면 느린 물건이다.
3. **읽기 속도 대비선.** 진짜 판정 기준은 "빠른가"가 아니라 **"내가 읽는 속도를 따라잡는가"**다.
   기준선을 겹쳐 그리면 결론이 판정문으로 바뀐다 — *"35 t/s는 한국어 읽기 속도에 아직 못 미친다"* /
   *"여기서부터는 더 올려도 체감 이득이 없다"*.

⇒ 제품 정의: **한국어 기준 · TTFT 포함 · 읽기 속도 대비 tok/s 체감기 (다국어 지원)**

## 3. MVP 범위

### 넣는다
- 3레인 동시 스트리밍 기본값 **5 / 10 / 35 tok/s** + 커스텀 슬라이더(1~500)
- **언어 토글 ko / en** — UI 문구와 샘플 텍스트, **그리고 토큰 밀도 계수가 함께 바뀐다** (핵심)
- 사용자 텍스트 붙여넣기 (자기 프롬프트 결과로 재보게)
- **TTFT 슬라이더(0~10초)** — 각 레인 시작 전 대기. 차별점의 절반
- **읽기 속도 기준선** — 레인 위에 겹치는 바 + "읽기 속도 도달/미달" 판정 문구
- 결과 공유용 URL 파라미터(`?tps=5,10,35&ttft=1.2&lang=ko`)
- 다크/라이트, 모바일 세로 레이아웃 (이런 페이지는 폰에서 열릴 확률이 높다)

### 안 넣는다 (MVP)
- 백엔드·DB·계정·분석 스크립트 — **전부 없다.** 정적 파일이 끝이다
- 실제 모델 호출 (시뮬레이션이지 추론기가 아니다)
- 정확 토크나이저 wasm (아래 4-2)

### v2 후보
- wasm 토크나이저로 근사 → 실측 전환
- 하드웨어 프리셋(3090+Qwen 등 실측치) — **커밋된 JSON**으로. API 만들지 마라
- 언어 추가 (ja/zh 도 토큰 밀도 이슈가 같다 — 확장 축이 이미 설계에 있다)

## 4. 기술 결정

### 4-1. 원본 참고 — 라이선스부터 확인해라 ⚠️
`jimmyboudoux/token-speed-visualizer` 를 **참고 기준**으로 삼는다 (바닐라·의존성 0·벽시계 타이밍 구조가 맞다).
**단 조사 시점에 리포에 라이선스 표기를 확인하지 못했다.** 착수 첫 작업으로 실제 리포를 열어
LICENSE 유무를 확인해라.
- 라이선스 있고 허용적 → 포크 후 축 3개(언어·TTFT·읽기속도)를 얹는다
- 없거나 불명 → **코드를 가져오지 말고 구조만 참고해 새로 짠다.** 어차피 수백 줄짜리다

### 4-2. 토큰화 — 근사로 시작하고 근사라고 화면에 적어라
정확 토크나이저(tiktoken/HF)를 브라우저에서 돌리려면 wasm + vocab 수 MB다. MVP엔 과하다.
- v1: **문자 종류별 계수**(한글 음절/조사, 라틴 단어, 숫자·문장부호)로 근사
- 계수는 **추측하지 말고 실측해서 근거를 남겨라** — 대표 모델 토크나이저로 한/영 샘플을 재고
  `docs/token-density.md` 에 표본·수치·측정일을 적는다 (판정 증거는 판정 시점에 박는다)
- 화면에 "근사치 / 기준 모델 / 측정일"을 표시한다. **숨은 근사는 거짓말이 된다**
- 읽기 속도 기본값도 같다 — 한국어 묵독 속도 수치는 **출처를 찾아 적고**, 못 찾으면 슬라이더 기본값으로만 두고
  단정하지 마라

### 4-3. 스택
바닐라 HTML/CSS/JS. 빌드 스텝 없음, 런타임 의존성 0, 외부 요청 0(폰트 CDN도 쓰지 마라 — CSP 깨끗하게).
프레임워크를 넣고 싶어지면 그건 범위가 샌 신호다.

## 5. 호스팅 판단 (2026-09-01) — **결론: 자체 하드웨어에 올리지 않는다**

| 후보 | 판정 | 근거 |
|---|---|---|
| **Cloudflare Pages** | ✅ **채택** | *사용자가 기다리는 경로에 자체 하드웨어를 넣지 않는다*. 100% 사용자 대면인데 백엔드가 0 → 계층 0으로 보내는 게 계약에 정확히 맞다. Cloudflare 계정·도메인 이미 운용 중 |
| GitHub Pages | 🟡 차선 | 도메인 안 붙이고 던져볼 거면 더 가볍다 |
| 자체 서버 + 도커 + 터널 | ❌ | 정적 파일 하나에 컨테이너·ingress·유닛을 붙이는 건 순수 운영 부채다 |

- **도커는 어디에도 안 쓴다.** 개발 중 미리보기는 `python3 -m http.server 8000` 이면 끝이다.
- 나중에 라이브 벤치 API 같은 게 붙으면 그때 자체 하드웨어를 꺼낸다. 그 경우에도 프론트는 Pages,
  데이터는 커밋된 JSON이면 여전히 서버가 안 필요하다.

## 6. 미결 — 되돌릴 수 없는 결정

1. **GitHub 리포 생성 및 공개 여부** — 대외 게시다. 현재 이 폴더는 **로컬 git init 만** 돼 있고 리모트가 없다
2. **Cloudflare Pages 프로젝트 생성 / 배포** — 대외 게시
3. **커스텀 도메인·DNS** — 비가역. `*.pages.dev` 기본 도메인까지가 승인 없이 말할 수 있는 선이다

⇒ 승인 전까지 **로컬에서 완성도를 올리는 작업은 전부 자유롭게 해도 된다.**

## 7. 착수 순서 (제안)

1. 이 문서를 읽고 범위를 확인한다
2. `token-speed-visualizer` 리포 열어 **LICENSE 확인** (§4-1) → 포크/신규작성 결정
3. 토큰 밀도 계수 실측 → `docs/token-density.md` (§4-2)
4. 단일 페이지 MVP 구현 (§3) — 3레인 + TTFT + 읽기속도 기준선 + ko/en
5. 로컬 미리보기로 유지보수자에게 시연 → 피드백
6. 공개 여부 결재 요청 (§6)

---

### 부록: 이 문서에 없는 것

운영·인프라 판단은 여기 없다. 이 문서가 담는 것은 **무엇을 만드는가, 왜 그렇게 정했는가,
무엇이 아직 안 정해졌는가** 뿐이다. 그 셋이 아닌 내용이 여기 쌓이기 시작하면 범위가 샌 신호다.
